version: '3.8'

services:
  astro-ssr:
    build: .
    container_name: astro-ssr-container
    ports:
      - "4321:4321"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=production
    # ⬅️ Ejecuta tu archivo `entry.mjs`
    command: node dist/server/entry.mjs
